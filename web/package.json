{
  "name": "online-gacha-backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
  "migrate": "node-pg-migrate -d $DATABASE_URL -m ./migrations --create-schema --migrations-table migrations",
  "seed": "node-pg-migrate seed -d $DATABASE_URL -m ./seeds"
  },
  "dependencies": {
    "fastify": "^4.27.2"
  },
  "devDependencies": {
    "node-pg-migrate": "^7.7.0"
  }
}
